k = -9

if k>= 0:

    a= str(k) +"正数"

    print(a)

else:

     print(str(k)+ "负数")


#2k = -9   变量名必须以字母或下划线开头，但以下划线开头的变量在Python中有特殊含义；
#  if 2k>= 0:   同一个级别的代码块的缩进量必须相同。所以要去掉前面的空格

#    with = 2k +"正数"   with 是关键词不能作为变量名

#        print(with)    把with 改掉

#    else:     else应和上面的if位于同一级别，所以前面的空格要去掉。

#    print(2K+ '负数")   应用的符号应为""或‘’；字符串和数字在一起时应强制转化，转换为str。
