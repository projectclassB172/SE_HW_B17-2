"""
2k = -9 ->变量名必须以字母或下划线开头，且以下划线开头时有特殊含义，2k改k

  if 2k>= 0: ->同一个级别的代码块的缩进量必须相同，应与k对齐，2k改k

    with = 2k +"正数" ->不能使用关键字作变量名，with改w，int型不能直接与str型进行拼接先强转为str(k)

        print(with) ->应与w对齐，with改w

    else: ->应与if对齐

    print(2K+ '负数") ->变量名对英文字母的大小写敏感且不能直接拼接须强转，2K改str(k)，引号的开始与结束必须是相同类型的引号，'改"或"改'
"""

k = -9

if k>= 0:

    w = str(k) +"正数"

    print(w)

else:

    print(str(k)+ "负数")

#Run Module
#-9负数
    
